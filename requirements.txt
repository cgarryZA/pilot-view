# Pilot View runtime dependencies.
# Install with:  pip install -r requirements.txt
#
# The Orbbec camera binding (pyorbbecsdk) is NOT listed here because it is
# platform-specific and installed separately:
#   - Raspberry Pi (aarch64): built from source -> package "pyorbbecsdk2" (imports as pyorbbecsdk)
#   - Windows (x64):          prebuilt wheel from the Orbbec pyorbbecsdk releases
# The app imports it lazily, so synthetic/disconnected modes run without it.

fastapi
uvicorn[standard]
webauthn
numpy
opencv-python
scipy

# LifeOS integration — MCP server (mcp/server.py) + /widget endpoint
mcp[cli]>=1.0
httpx>=0.27
pydantic>=2
cryptography
